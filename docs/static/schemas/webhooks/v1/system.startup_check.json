{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://www.authelia.com/schemas/webhooks/v1/system.startup_check.json",
  "properties": {
    "username": {
      "type": "string",
      "title": "Username",
      "description": "The username the occurrence concerns."
    },
    "display_name": {
      "type": "string",
      "title": "Display Name",
      "description": "The display name recorded in the authentication backend."
    },
    "emails": {
      "items": {
        "type": "string"
      },
      "type": "array",
      "title": "Emails",
      "description": "The email addresses recorded in the authentication backend."
    },
    "remote_ip": {
      "type": "string",
      "title": "Remote IP",
      "description": "The client address which caused the occurrence."
    },
    "probe": {
      "type": "boolean",
      "title": "Probe",
      "description": "Always true. Marks this as a connectivity probe rather than an occurrence, so a receiver can discard it."
    }
  },
  "type": "object",
  "required": [
    "username",
    "probe"
  ],
  "description": "DataStartupCheck is the payload for the system.startup_check event."
}
