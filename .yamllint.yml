---
extends: default

ignore: |
  internal/configuration/test_resources/config_bad_quoting.yml
  docs/pnpm-lock.yaml
  web/pnpm-lock.yaml
  web/node_modules/

rules:
  document-end:
    level: warning
  empty-values:
    level: warning
  indentation:
    spaces: 2
    check-multi-line-strings: true
  line-length:
    max: 120
  octal-values: enable
...
