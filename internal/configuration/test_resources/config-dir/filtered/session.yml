---
session:
  name: 'authelia_session'
  expiration: '1 hour'
  inactivity: '5 minutes'
  domain: '{{ env "ROOT_DOMAIN" }}'

cache:
  redis_sentinel:
    master_name: 'test'
    addresses:
      - 'tcp://${SERVICES_SERVER}:6379'
...
