import { type Config } from "prettier";

const config: Config = {
	quoteProps: 'consistent',
	singleQuote: true,
	trailingComma: 'all',
	printWidth: 80,
	useTabs: false,
	bracketSpacing: true,
};

module.exports = config;
