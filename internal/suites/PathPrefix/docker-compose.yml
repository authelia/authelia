---
version: '3'
services:
  authelia-backend:
    volumes:
      - './PathPrefix/configuration.yml:/config/configuration.yml'
      - './PathPrefix/users.yml:/config/users.yml'
      - './common/pki:/pki'
...
