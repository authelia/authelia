---
version: '3'
services:
  authelia-backend:
    volumes:
      - './OneFactorOnly/configuration.yml:/config/configuration.yml'
      - './OneFactorOnly/users.yml:/config/users.yml'
      - './common/pki:/pki'
...
